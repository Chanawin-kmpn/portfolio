import React from "react";
import { Container } from "../../styles/ContainerStyle";

const MySkillSection = ({ children }) => {
  return <Container>{children}</Container>;
};

export default MySkillSection;
